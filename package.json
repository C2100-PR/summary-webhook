{
  "name": "summary-webhook",
  "version": "1.0.0",
  "description": "Cloud Function webhook for processing text summaries",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=summary-webhook",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.0",
    "@google-cloud/logging": "^10.4.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}